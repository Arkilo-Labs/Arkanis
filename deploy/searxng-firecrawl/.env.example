# 绑定到本机回环，避免在服务器上误暴露
BIND_IP=127.0.0.1

# 端口与本项目默认配置对齐：
# - SearXNG: http://localhost:8080
# - Firecrawl: http://localhost:3002
SEARXNG_PORT=8080
FIRECRAWL_PORT=3002

# Firecrawl 队列面板鉴权 key（第一次 up 会自动生成并写入 .env）
BULL_AUTH_KEY=CHANGEME

# 可选：覆盖镜像来源（当 GHCR 拉取被拒绝/网络受限时使用）
# REDIS_IMAGE=redis:7-alpine
# SEARXNG_IMAGE=searxng/searxng:latest
# PLAYWRIGHT_IMAGE=ghcr.io/firecrawl/playwright-service:latest
# FIRECRAWL_IMAGE=ghcr.io/firecrawl/firecrawl:latest
# NUQ_POSTGRES_IMAGE=ghcr.io/firecrawl/nuq-postgres:latest
# RABBITMQ_IMAGE=rabbitmq:3-management

# 可选：避免与其他项目冲突（容器名默认 searxng，匹配代码里的 docker fallback）
# SEARXNG_CONTAINER_NAME=searxng

# （可选）如果要用 Firecrawl 的 /extract 或其它依赖 LLM 的能力，再配：
# OPENAI_API_KEY=
# OPENAI_BASE_URL=
# MODEL_NAME=
# MODEL_EMBEDDING_NAME=

# （可选）细化 Firecrawl 使用 SearXNG 的检索行为
# SEARXNG_ENGINES=
# SEARXNG_CATEGORIES=
# SEARXNG_LANGUAGES=
# SEARXNG_LIMIT=
# SEARXNG_TIME_RANGE=
# SEARXNG_SAFESEARCH=
