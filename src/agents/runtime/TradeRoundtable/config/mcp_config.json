{
  "version": 1,
  "mcpServers": {
    "trendradar": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "--directory",
        "${ENV:TRENDRADAR_DIR}",
        "run",
        "python",
        "-m",
        "mcp_server.server"
      ]
    }
  }
}
